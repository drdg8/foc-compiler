int main() {
    // For Loop
    for (int i = 0; i < 5; i++) {
        printf("For loop iteration: %d\n", i);
    }

    // While Loop
    int count = 0;
    while (count < 5) {
        printf("While loop iteration: %d\n", count);
        count++;
    }

    return 0;
}
