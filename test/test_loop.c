int main() {
    // For Loop
    int i;
    for (i = 0; i < 5; i = i+1) {
        printf("For loop iteration: %d\n", i);
    }

    // While Loop
    int count = 0;
    while (count < 5) {
        printf("While loop iteration: %d\n", count);
        count = count + 1;
    }

    return 0;
}
